server {
  listen {{ joanie_admin_nginx_healthcheck_port }};
  server_name localhost;

  location {{ joanie_admin_nginx_healthcheck_endpoint }} {
    add_header Content-Type text/plain;
    return 200 "OK";
  }

  location {{ joanie_admin_nginx_status_endpoint }} {
     stub_status;
  }

}
